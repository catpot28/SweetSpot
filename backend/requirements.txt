# Runtime: Python 3.12
# Pin versions before production. For hackathon, loose bounds are fine.

httpx                # async HTTP client for BUNQ / SerpApi / ImgBB / Telegram
cryptography         # RSA request signing for BUNQ
fastapi              # web framework
uvicorn[standard]    # ASGI server (with watchfiles + uvloop)
pydantic-settings    # env-var loading in app/core/config.py
asyncpg              # async Postgres driver for Supabase

# Added as modules come online:
#   python-telegram-bot   # (or raw httpx — decide later)
#   anthropic             # Claude API client
#   python-multipart      # FastAPI file uploads for /lens/scan
#
# Dev-only (move to requirements-dev.txt later):
#   pytest
#   pytest-asyncio
#   respx                 # mock httpx in tests
#   ruff                  # lint
