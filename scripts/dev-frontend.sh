#!/usr/bin/env bash
# Runs the Vite dev server for the React PWA.
# Expects: frontend scaffolded with package.json and node_modules installed (see setup.sh).
# TODO: implement once frontend is scaffolded.

set -euo pipefail
echo "dev-frontend.sh — not yet implemented"
