#!/usr/bin/env bash
# Runs the FastAPI backend locally with auto-reload.
# Expects: Python 3.12 venv at backend/.venv with requirements installed (see setup.sh).
# Loads env vars from .env at the repo root.
# TODO: implement once backend/app/main.py exists.

set -euo pipefail
echo "dev-backend.sh — not yet implemented"
